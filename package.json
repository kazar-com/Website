{
  "name": "kazar-website-implementation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node --env-file-if-exists=.env server/local.mjs",
    "dev": "node --env-file-if-exists=.env server/local.mjs --agent-preview",
    "test": "node --test tests/*.test.mjs"
  }
}
