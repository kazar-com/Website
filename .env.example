# Copy to .env. Local review only. No provider credentials required.
HOST=127.0.0.1
PORT=3000
DATA_DIR=.data
APP_ORIGIN=http://127.0.0.1:3000
# Non-loopback binding requires both credentials; configure securely, not in chat.
STAGING_USER=
STAGING_PASSWORD=
